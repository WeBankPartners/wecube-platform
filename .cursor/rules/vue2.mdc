---
description: Vue 2.0 组件开发约定
globs: wecube-portal/src/**/*.vue
---

# Vue 2.0 开发规范

## 目录与命名
- 组件使用 `kebab-case` 命名文件，内部使用 `PascalCase` 注册
- 单文件组件按「模板-脚本-样式」顺序排列
- 每个目录包含 `index.vue` 时需提供 README 说明入口用途

## 组件封装规范
- 每个组件模板最外层必须添加唯一且语义化的 `class`，推荐格式 `wc-{domain}-{component}`，便于样式隔离与定位
- 单个 `.vue` 文件总行数不得超过 1000 行，超出应按照职责拆分子组件或提取逻辑模块
- 通用展示、交互或数据处理能力优先封装为复用组件/工具，并集中目录维护、提供 Demo 及文档示例

## 模板编写
- 使用简洁模板，尽量将逻辑放在 `computed` 或方法中
- 绑定 class/style 时使用对象或数组语法，避免三目运算
- 使用 `key` 跟踪列表，首选稳定业务主键
- 条件渲染优先 `v-if/v-else`，频繁切换场景再使用 `v-show`
- **模板注释**：在 `<template>` 中使用 HTML 注释（`<!-- -->`）标注重要功能区域：
  - 主要功能区块（如：`<!-- 用户列表区域 -->`、`<!-- 搜索表单区域 -->`）
  - 复杂的条件渲染块（如：`<!-- 当用户有权限时显示 -->`）
  - 重要的业务逻辑区域（如：`<!-- 数据导出功能 -->`）
  - 表单区域及其用途（如：`<!-- 基本信息表单 -->`、`<!-- 高级配置表单 -->`）
  - 复杂的列表渲染（如：`<!-- 动态渲染的角色列表 -->`）
  - 弹窗/抽屉区域（如：`<!-- 新增用户弹窗 -->`、`<!-- 编辑角色抽屉 -->`）
  - 需要解释的重要事件绑定或数据绑定
  - 没有上下文可能难以理解的结构区域

## 脚本约定
- 使用 Options API，`data` 返回工厂函数
- `methods` 内函数名使用动宾结构，事件处理函数以 `handle` 前缀
- 优先使用计算属性派生状态，复杂逻辑拆为辅助函数
- 避免直接修改 props，必要时复制到本地数据
- 使用早返回减少嵌套

## 注释规范
- **中文注释**：所有代码注释必须使用中文（简体）编写
- **组件注释**：在组件 `<script>` 标签开头添加中文注释，说明组件的用途、主要功能和业务场景
- **模板注释**：在 `<template>` 中使用 HTML 注释（`<!-- -->`）标注重要功能区域和业务逻辑区块，帮助理解模板结构和功能划分
- **方法注释**：每个 `methods` 中的方法顶部使用中文注释说明其功能、参数和返回值
- **计算属性注释**：复杂的 `computed` 属性添加中文注释解释其计算逻辑和依赖关系
- **生命周期注释**：在生命周期钩子函数中添加中文注释说明其执行时机和主要操作
- **Props 注释**：在 `props` 定义中为每个属性添加中文注释说明其用途和类型要求
- **复杂逻辑注释**：对于复杂的业务逻辑、数据处理或状态转换，必须添加中文注释解释其原理
- **TODO 注释**：使用 `// TODO: 中文说明` 格式标记待办事项和已知问题

## 数据流与通信
- `props` 明确 `type`、`default`、`required`，必要时补充 `validator`
- `$emit` 事件全部声明在 `emits` 数组/对象中
- 父子通信尽量保持单向数据流，跨层级使用事件总线或 Vuex
- 注入的依赖通过 `provide/inject` 显式声明，注明只读或可写

## 状态管理
- Vuex 模块按领域拆分，state 保持最小必需
- Mutation 只做同步更新，异步逻辑放在 Action
- 使用常量定义 Mutation/Action 名称，避免魔法字符串

## 样式约定
- 默认启用 `scoped`，需要全局覆盖时在注释中说明
- 优先使用 BEM 命名，避免层级选择器
- 变量、混入统一来自主题/变量文件

## 性能注意
- 组件拆分控制在必要粒度，重型组件懒加载
- Watcher 添加 `immediate` 或 `deep` 时写注释说明原因
- 长列表使用虚拟滚动或分页

## 可测试性
- 对外暴露方法保持纯粹，方便单元测试
- 组件关键路径提供最少 1 个测试用例（Vue Test Utils）
