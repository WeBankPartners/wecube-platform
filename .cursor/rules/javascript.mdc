---
description: JavaScript 语法与风格指南
globs: wecube-portal/src/**/*.js, wecube-portal/src/**/*.mjs, wecube-portal/src/**/*.cjs
---

# JavaScript 编码规范

## 语言特性
- 默认使用 ES2019+ 语法，禁止使用已废弃特性
- 必须开启严格模式，模块文件天然处于 strict 环境
- 优先使用 `const`，仅在确需重赋值时使用 `let`，禁用 `var`
- 使用模板字符串代替字符串拼接

## 结构与可读性
- 每个模块保持单一职责，文件长度控制在可读范围
- 函数顶部添加注释，说明用途、输入输出、副作用
- 复杂逻辑先写伪代码/注释，再实现
- 使用早返回减少嵌套；条件分支超过 3 个考虑策略映射

## 注释规范
- **中文注释**：所有代码注释必须使用中文（简体）编写，包括函数说明、行内注释、TODO 标记和文档注释
- **函数注释**：每个函数顶部使用中文注释说明其功能、参数含义、返回值及可能的副作用
- **复杂逻辑注释**：对于复杂算法、业务逻辑或非直观的实现，必须添加中文注释解释其原理和意图
- **JSDoc 注释**：关键函数使用 JSDoc 格式，但注释内容使用中文，例如：
  ```javascript
  /**
   * 获取用户信息
   * @param {string} userId - 用户ID
   * @returns {Promise<Object>} 用户信息对象
   */
  ```
- **行内注释**：在代码行尾或上方使用中文注释解释关键步骤，避免过度注释显而易见的代码
- **TODO 注释**：使用 `// TODO: 中文说明` 格式标记待办事项

## 命名约定
- 变量、函数使用 `camelCase`，常量使用 `SCREAMING_SNAKE_CASE`
- 事件处理函数统一 `handleXxx` 命名
- Promise 返回值使用动词+名词，如 `fetchUser`

## 函数与对象
- 函数维持纯函数特性，必要副作用需注释
- 可复用逻辑抽为纯函数或工具模块
- 对象解构时提供默认值，避免 `undefined`
- 使用对象展开/数组展开替代 `Object.assign` 与 `concat`

## 控制流
- 使用可选链与空值合并运算符简化判空
- 异步逻辑统一 `async/await`，异常通过 `try/catch` 捕获
- 处理 Promise.all 时考虑局部失败，必要时使用 `allSettled`

## 错误处理
- 自定义错误使用继承 `Error` 的类
- 捕获异常后写清楚 fallback 或上抛原因
- 禁止空 `catch`，至少记录日志

## 测试与类型
- 新增模块需配套最小单元测试
- 使用 JSDoc 标注关键函数输入输出，提升类型推断
- 在边界场景补充断言，失败时给出可读信息

## 性能
- 避免在循环中创建未复用对象/函数
- 频繁 DOM 操作合并到 `requestAnimationFrame` 或批量更新
- 使用节流/防抖处理高频事件
