server:
   port: 8081
   servlet:
      context-path: /auth
logging:
   config: classpath:logback-spring.xml
spring:
   datasource:
      url: jdbc:mysql://localhost:port/db?useUnicode=true&characterEncoding=UTF8&useJDBCTCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=Asia/Shanghai&useSSL=false
      username: root
      ***REMOVED***
#      url: ***REMOVED***
#      username: wecube
#      password: password
      driver-class-name: com.mysql.cj.jdbc.Driver
   profiles:
      active: dev
   thymeleaf:
      cache: false
   jpa:
      database: mysql
      show-sql: true
      hibernate:
         ddl-auto: none
         use-new-id-generator-mappings: false